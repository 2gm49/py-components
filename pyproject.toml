[project]
name = "DiscordPYComponents"
version = "0.2.31"
description = "Discord Components V2 for discord.py"
authors = [{name="2gm49"}]
dependencies = ["aiohttp"]
readme = "README.md"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
